# Dependências com versões fixadas (v0.16.1): um `pip install` daqui a
# seis meses instala EXATAMENTE o que foi testado nesta versão — builds
# reprodutíveis. Para atualizar: subir a versão aqui, correr a suite, e
# só então fazer commit. (Ferramentas de lint: requirements-dev.txt.)
fastapi==0.139.2
uvicorn[standard]==0.51.0
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1
requests==2.33.1
beautifulsoup4==4.14.3
reportlab==4.4.10
